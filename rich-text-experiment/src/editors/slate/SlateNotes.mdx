Extremely flexible customizable, however no options available out of the box. Even simple elements like bold, and italic formatting have to be defined.

### Output

Generates an AST from the input and saves the data as a JSON object.

### Toolbar and Keybindings

Available, but must be configured.
